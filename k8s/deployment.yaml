apiVersion: apps/v1
kind: Deployment
metadata:
  name: passion-mansour-teambeam-frontend
spec:
  selector:
    matchLabels:
      app: passion-mansour-teambeam-frontend
  template:
    metadata:
      labels:
        app: passion-mansour-teambeam-frontend
    spec:
      containers:
        - name: passion-mansour-teambeam-frontend
          image: krmp-d2hub-idock.9rum.cc/dev-test/repo_d8e1a2a6d74b
          imagePullPolicy: Always
          command: [ "/bin/sh", "-c", "env" ]
          envFrom:
            - secretRef:
                name: envsecret